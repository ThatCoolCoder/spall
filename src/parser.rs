use std::vec;

pub struct Node {
    tag_name: String,
    children: Vec<Node>
}

pub fn parse_element(element: &str) -> Vec<Node> {
    // Tokenise the element then arrange it in a hierarchy

    return vec!();
}